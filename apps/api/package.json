{
  "name": "@cadence/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -b",
    "start": "node dist/server.js",
    "test": "cross-env DATABASE_URL=\"file:./test.db\" prisma db push --accept-data-loss && cross-env DATABASE_URL=\"file:./test.db\" vitest run --pool=forks --no-file-parallelism",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cadence/shared": "*",
    "@fastify/cors": "^9.0.1",
    "@prisma/client": "^5.17.0",
    "dotenv": "^17.4.2",
    "fastify": "^4.28.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "cross-env": "^10.1.0",
    "prisma": "^5.17.0",
    "tsx": "^4.19.1",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  }
}
